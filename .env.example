# Local processor mode. Keep this as local when running app.py on your machine.
APP_MODE=local

# Required only when a semantic classification is not already present in llm_cache.json.
GEMINI_API_KEY=

# Optional: change only if you intentionally want a different Gemini model.
GEMINI_MODEL=gemini-2.5-flash-lite
